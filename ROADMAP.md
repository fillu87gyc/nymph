# nymph ロードマップ

> スタック: Bun · React 19 · TypeScript · Vite 8  
> 配布: `bunx nymph` / `bun build --compile`
>
> 実装済み機能の一覧 → [docs/features.md](docs/features.md)

---

## 完了（2026-08-13 セッション）

- **キーボードショートカット（小粒 UX タスク #1）**: `?` でショートカット一覧、`C` でコメントパネル開閉、`T` でテーマ切替を足した。判定と一覧の定義は `src/client/lib/shortcuts.ts`、一覧の画面は `ShortcutsModal`
  - **判定を純関数（`matchShortcut`）に切り出した**。「IME 変換中は拾わない」「入力欄では拾わない」のような条件は、壊れても画面を見ているかぎり気付けない類のもの。document の購読側に埋めると単体で確かめられなくなるため、キーの解釈だけを外へ出して単体テストで固めている
  - **一覧はこの定義（`SHORTCUT_SECTIONS`）をそのまま並べる**。実装と説明を二重管理すると必ずずれるので、`matchShortcut` が返しうるアクションが漏れなく一覧に載っていることを単体テストで見張る形にした。Esc や配置画面の矢印キーのように各画面が自前で扱うキーも、利用者から見れば同じキーボード操作なので同じ 1 枚に載せている
  - 修飾キー無しの 1 文字は **Ctrl / Cmd との同時押しでは反応させない**（`Ctrl+T` = 新しいタブ、`Cmd+C` = コピーを奪わない）。入力欄にフォーカスがあるあいだと、他のモーダルが出ているあいだも拾わない——読んでいる / 打っている裏で背後の画面が変わるのを避けるため。Ctrl/Cmd+P（Quick Open）だけは従来どおり、開いていても閉じるために効かせる
  - `?` はキーを知らなければ辿り着けないので、「⋯」メニューに **⌨ ショートカット一覧** の口も置いた
  - コメントパネルを枠に置いているときは常時表示（`panelOpen` を見ない）ため、`C` では開閉しない。一覧の説明文にもそう書いてある
- **画面からのエクスポート（v0.4 の宿題）**: 「⋯」メニューに **エクスポート**（📄 HTML / 📝 Markdown / 📊 CSV）を足し、CLI の `--export` / `--annotate` / `nymph export` と同じ 3 形式をブラウザから直接ダウンロードできるようにした。窓口は `GET /export?file=&format=`（`src/server.ts`）、中身の組み立ては `src/exportPayload.ts`
  - これまで画面から結果を持ち出す手段は「印刷 → PDF」だけで、コメント入りの配布物が欲しければターミナルへ戻る必要があった。レビューしている画面のまま出せる形にした
  - **中身は CLI とまったく同じ**にする。`exportPayload.ts` は `htmlExport` / `markdownAnnotate` / `csvExport` へ委譲するだけで、独自の整形を一切持たない（経路によって解釈が変わるのが一番困る。単体テストで CLI の `--export` とバイト一致を見ている）
  - **生成物はサーバーのディスクに残さない**。書き出し先を決めるのはブラウザで、サーバーはレスポンスとして返すだけ。画面を触っただけでレビュー対象の隣にファイルが増える、を作らない
  - 出力名は `<元の名前>-review.<拡張子>`。Markdown は元ファイルと拡張子が同じになるため、名前の側でも「レビュー対象は書き換えない」という約束を守る。日本語のファイル名は `Content-Disposition` の `filename*`（RFC 5987）に載せる
  - Mermaid の同梱はチェックボックスで選ばせる（既定 off ＝ CLI の `--export-mermaid` と同じ）。図が 1 つも無い文書では 3MB のバンドルを読みに行かない——判定は `containsMermaid()` として `reviewBlocks.ts` に置き、ブロック分割と同じ lang 見分けを使う
  - CSV だけは CLI と既定を逆にして **BOM 付き**で落とす。CLI の既定（BOM 無し）は標準出力へ流してパイプに繋ぐ経路を前提にしたもので、ブラウザからのダウンロードは行き先がほぼ表計算ソフトのため
  - 対象にできるのは開いているファイルだけ（`/content` と同じ認可）。ドロップ由来の擬似タブはファイル実体もコメントの保存先も無いので、項目を無効化したうえでサーバー側でも断る

---

## 完了（2026-08-09 セッション）

- **コメントを Markdown に書き戻し（v0.4 #3）**: `nymph <file> --annotate <out.md>` で、保存済みコメントを本文の各ブロック直後へ `> [nymph] 未解決 · L3 · ラウンド 2 · 2026-08-09 10:20` の引用として挿し込んだ Markdown を書き出せるようにした（`--export` と同じく、サーバーは起動せず既存インスタンスへの委譲にも乗せない）。組み立ては `src/markdownAnnotate.ts`（純関数）、ファイル入出力は `src/annotateCommand.ts`
  - HTML エクスポートが「読ませる配布物」なのに対し、こちらは**そのまま編集を続けられる形**でレビューを返すための出力。受け手（人でも AI でも）は本文の隣に指摘がある Markdown を受け取り、直してそのまま次のラウンドへ進める
  - **本文の行は書き換えない**。足すのは引用と、それを区切るための空行だけにした。引用の前後の空行は見た目のためではなく必須——空行が無いと直後の本文が引用の lazy continuation として吸われ、**元の文書の意味が変わる**。同じ理由で、引用の中もヘッダー / 対象 / 本文のあいだに `>` の空行を挟む（引用内の連続行は 1 段落にまとめられるため、空けないと「[nymph] 未解決 · L3 主語が曖昧です」と地続きに表示される）
  - 「対象」の引用は、直前のブロックだけでは何を指しているか分からないもの（選択・表・コード・差分）にだけ添える。段落や見出しへの指摘で本文をもう一度書き写しても増えるのはノイズだけなので出さない
  - 行番号は**元ファイル基準**。引用を挿し込んだ時点で以降の行はずれるため、書き戻した Markdown 自身の行番号とは一致しない（本文と指摘を同じ 1 枚に載せる以上、避けようがない。README にも明記した）
  - **元ファイルへの上書きは拒否する**。「ツールの状態はレビュー対象の外に置き、対象を汚さない」という設計原則をここでも守り、書き戻しは別ファイルへの出力に限る（`--annotate-open` で解決済みを外せる。既定は全件——書き戻しから黙って情報が落ちる方が驚きが大きいため）
- **JSON → CSV エクスポート（v0.4 #4）**: `nymph export <file.md> [-o <out.csv>] [--bom]` で、保存済みコメントを 1 件 1 行の CSV にできるようにした（`src/csvExport.ts` = 純関数、`src/csvCommand.ts` = 入出力）。起動オプションではなくサブコマンドにしたのは、これがレビュー対象を開く行為ではなく「溜まったデータを別形式で吐く」一発仕事のため
  - RFC 4180（CRLF・`"` は `""`・必要なときだけ引用）に従い、列は `file, id, status, line_start, line_end, block_type, round, created_at, target, comment`。見出しと `status` の値は英字にした（人向けの表示ではなく後段の処理が読む列名のため）
  - `-o` を省くと書き出さず標準出力へ流す（`nymph export a.md | …` を通すため、そのときは他のログを一切混ぜない）。BOM は既定で付けず `--bom` を選ばせる——Excel には BOM が要るが、パイプの先には邪魔なため
  - `=` `+` `@`・タブ・CR で始まる値は先頭に `'` を足して無害化する（レビューコメントに混ざった `=HYPERLINK(…)` を Excel / Sheets が数式として評価してしまう）。`-` は対象外にした。数式にはならない一方、日本語のコメントは箇条書きの `- ` で始まることが多く、そちらを潰す実害の方が大きい
- **ブロック分割の共有（`src/reviewBlocks.ts`）**: 上の 2 経路を足すにあたり、`htmlExport.ts` にあった「本文をコメントが紐づく単位へ割る / 対象が消えたコメントを洗い出す」処理を切り出して 3 経路で共有した。出力ごとに独自に数えると、同じコメントが出力によって別の場所に付いたり片方だけ「削除済」になったりする。コメントの割り当ても `anchorComments` に集約している

---

## 完了（2026-08-06 セッション）

- **配置画面の中身プレビューと「今は非表示」の注記（ウィジェットの宿題）**: 配置画面（`WidgetArrangeScreen`）の左右の枠のチップに、そのウィジェットが実際に並べている中身の先頭 3 件と総数を添えた。置いてみるまで何が出るのか分からなかったのを、置く前の判断材料ごと出す形にしている
  - プレビューはウィジェット本体を縮小描画するのではなく、本体と同じ純関数（`docScan` など）から中身だけを取り出して並べる（`src/client/lib/widgetPreview.ts`）。本体をもう一度描くと `data-testid` が画面に二重で現れて既存のテストが壊れ、Mermaid の描画や `/search` のリクエストも二重に走るため——図のサムネイルを見送ったときと同じ判断
  - 一覧に馴染まないもの（検索・ミニマップ・文書統計）は件数ではなく代わりの一言を出す。中身が空のウィジェットは、本体が出す「ありません」と揃った文言をプレビューにも出す
  - 枠に置いてあるのに条件を満たさず今は出ていないもの（ルート未指定のエクスプローラー、閉じているアウトライン / コメント）には、理由と出し方（「フォルダを開く」で指定、など）を添える。枠に置いたのに画面に現れないと配置が効いていないように見えるため
  - 表示判定は `widgetVisibility` に集約し、`App.renderWidget`（実際に描くか）と配置画面の注記が同じ関数を見る。走査を伴うプレビューの構築は配置画面を開いているあいだだけ走らせる
- **印刷 / PDF（v0.4 #2）**: アプリ画面をそのまま紙 / PDF にできるようにした。「⋯」メニューの **🖨 印刷 / PDF**（`window.print()` を呼ぶだけ）と、紙向けの体裁を持つ `src/client/styles/print.css`（`@media print`）。Ctrl / Cmd + P からでも同じ結果になる
  - 紙に出すのは本文だけ。セレクタは「残すものを指定して、それ以外を落とす」形（`body > *:not(#root)` / `#app > *:not(#main)` / `#main > *:not([data-print-region])`）にした。ウィジェットやモーダルが増えても印刷側に漏れない——列挙する形だと、新しい枠を足すたびに印刷 CSS へ追記が要り、忘れても画面を見ているかぎり気付けないため
  - 配色は印刷時だけライトに倒す。ブラウザは既定で背景色を印刷しないので、ダークのままだと「白い紙に薄い灰色の文字」になる。`beforeprint` で `data-theme` を書き換える案は採らなかった（hljs のテーマは `<link>` の差し替えなので、印刷直前に触ると読み込みが間に合わないまま印刷が始まりうる）。代わりに値を書き写し、`tests/unit/printStyles.test.ts` が `[data-theme="light"]` との一致を機械的に見張る
  - 画面は `html/body` が `overflow:hidden` で内側の列だけスクロールする作りなので、そのままでは 1 ページ目で切れる。印刷時はスクロール容器（`.app` / `.mainRow` / `.contentGrid` …）を解いて紙の高さに流す
  - コードのシンタックスハイライトは紙では落とす（背景が印刷されない紙の上でコントラストを保証できない。HTML エクスポートにハイライトを持たせていないのと同じ判断）。長いコード行は折り返し、コード・表・図はページの切れ目をまたがせない
  - コメントは紙に出さない。コメント入りの配布物は `--export` の HTML（印刷用 CSS 入り）を印刷する経路に一本化した

---

## 完了（2026-08-05 セッション）

- **HTML エクスポート（v0.4 #1）**: `nymph <file> --export <out.html>` で、本文と保存済みコメントを 1 枚の静的 HTML に書き出せるようにした（サーバーは起動せず、既存インスタンスへの委譲にも乗せない）。組み立ては `src/htmlExport.ts`（ほぼ純粋な文字列生成）、ファイル入出力は `src/exportCommand.ts` に分けている
  - 要件は「生成物が**単体で完結する**」こと。CSS / JS はインライン、画像はデータ URI（対象ファイルのディレクトリ配下・1枚 2MB まで、範囲外と外部 URL は元の src のまま）で、外部ホストを一切参照しない。メール添付・社内 Wiki 貼り付け・そのまま印刷を想定し、印刷用 CSS も入れた
  - コメントは対象ブロックの直後に置く（状態 / 行番号 / ラウンド / 「もとの文章」つき）。対象が消えたものと差分への指摘は末尾の「本文に紐づかないコメント」へまとめる。状態判定（未解決 / 削除済 / 解決済）は `commentStatus` を、行番号の割り当ては `assignLines` / `getBlockTokensDFS` をアプリと共有し、アンカーがずれないようにした
  - **Mermaid は `--export-mermaid` で描画エンジンごと同梱**（既定はソース表示のまま）。`dist/mermaid-standalone.js`（mermaid 配布の UMD ビルドをビルド時に複製）を丸ごと焼き込み、オフラインのまま図が描かれる。図が無い文書には積まない。描画に失敗した図はソース表示のまま残す（出発点をソース表示にして、成功したものだけ差し替える設計。JS 無効でも構文エラーでも情報が欠けない）
    - CDN から拾う案は採らなかった。(1) 配布物が開かれるたび第三者へ接続する＝誰がいつ読んだかが漏れる、(2) mermaid の ESM ビルドは図の種類ごとに `./chunks/*` を動的 import するため entry に SRI を付けても守りきれない、(3) 完了済みの「hljs テーマ CSS の同梱」で一度 CDN 直リンクを捨てた経緯と逆行する——の3点
  - アプリとの意図的な差: 本文中の生 HTML は sanitize せず**エスケープして literal 表示**する（DOMPurify はブラウザの DOM が要る。「消毒し損ねた HTML を配布物に埋める」より安全側に倒した）。シンタックスハイライトは無し
  - `marked` を devDependencies から dependencies へ移した（サーバー側の実行時依存になったため）。あわせて `package.json` の `files` を `src` 全体に直している（`src/cli.ts` が import する `globScan.ts` などが公開物に含まれておらず、以前から publish 後に壊れる状態だった）
- **ウィジェット枠の幅ドラッグ**: 左右の枠の幅（左240px / 右220px 固定だった）を、枠と本文の境目のドラッグで変えられるようにした。幅のモデルは `src/client/lib/slotWidth.ts`（下限140px / 上限480px / 既定は移設前と同じ 240・220px、`localStorage` の `nymph-slot-width` へ左右別々に保存）。ハンドルは枠の内側の境界に重ねた `role="separator"` で、左右キー（16px ステップ）とダブルクリック / Home キーでの既定幅リセットも持つ
  - `ContentResizer`（本文左右端のドラッグ）との責務分担は「掴む場所で分ける」に統一した。枠と本文の境目を掴む＝サイドバーの幅が変わる（本文の行長は動かない）、本文列の端を掴む＝本文の行長が変わる（サイドバーには触らない）。見た目・キー操作は両者で揃えている

---

## 完了（2026-08-04 セッション）

- **ウィジェット第2弾（10種）**: 枠に置ける中身を、第1弾の4枚（タブ / エクスプローラー / アウトライン / コメント）から下表の10種へ拡張した。配置モデルにウィジェットの第3の種類——「既定位置を持たず、枠に置いたときだけ出る」もの——を足している（`WidgetMeta.slotOnly` → `required` に置き換え。`required` はエクスプローラー / アウトラインのみで、これらは従来どおり枠から出せず正規化で補完される。第2弾の10種は `required: false` + `defaultLabel: null` で、枠から出す＝画面から消す）。表示条件のトグルは持たせず「枠に置く＝出す」に統一し、中身が空のときは各ウィジェットが自分で「ありません」を出す（置いたのに何も現れない、を避ける）
  - 本文から作る一覧は `lib/docScan.ts`（タスク / リンク / 図 / 統計 / frontmatter / 用語の出現）に純関数でまとめた。marked のトークン化ではなく行単位の軽い走査（コードフェンスと frontmatter の中は対象外）で、本文更新のたびに作り直しても安い
  - ミニマップは `lib/minimap.ts`。1 行 = 1 本の棒（種別で色・文字数で長さ）に潰した疑似ミニマップで、300 本を超える文書は複数行を 1 本に束ねて DOM を一定に保つ。コメント位置を点で重ね、本文のスクロールに追従する枠とクリックジャンプを持つ
  - リンクの生死チェックだけサーバーが要る: `POST /link-check`（`src/linkCheck.ts`）。相対パスを開いているファイルのディレクトリ基準で解決し、**ルート配下（無ければそのディレクトリ配下）に限って**存在を返す。範囲外・外部 URL は `exists: null`（未確認）で、任意パスの存在を答える窓口にはしない
  - ジャンプ先の解決（`App.tsx` の `scrollToLine`）を「その行から始まるブロック → その行を含む最も内側のブロック → 直後のブロック」の順に拡張した（アウトラインの見出しジャンプはそのまま、タスク・リンク・ミニマップのようにブロックの途中や隙間を指す呼び出しに対応）
- **ウィジェット配置画面（第1弾の続き）**: 配置の変更を、設定ポップオーバー内のセグメントコントロール（「選んだ枠の末尾に追加」しかできなかった）から、専用の全画面 UI（`WidgetArrangeScreen`）へ移した。⚙ 設定 →「配置を編集」で開き、左に「利用可能」（＝枠に入れず既定位置に出すもの）一覧、右に 左枠 / 本文 / 右枠 のプレビューを並べ、HTML5 ドラッグ＆ドロップで行き来させる。枠の中は差し込み位置（gap）へ落とすと上下の順番が変わり、gap を外して落とすと末尾に積まれる。モデル側は `moveWidget(layout, id, placement, index)`（index = 対象を抜いたあとの配列での挿入位置）を追加し、既存の `placeWidget` はその末尾版として維持。キーボードでも同じ操作ができる（← → で枠を移動 / ↑ ↓ で並べ替え、移動後もフォーカスは同じチップに残り、結果は `aria-live` で読み上げる）。「初期配置にリセット」で初期状態へ戻せる。変更は他の表示設定と同じく即時反映＋保存で apply/cancel は持たない（閉じるボタンは「✓ 完了」）
- **ウィジェット配置（第1弾: 枠組み）**: 画面左右のスロット（`WidgetSlot`）に、既存の4パネル（タブ / エクスプローラー / アウトライン / コメント）を自由に積めるようにした。配置モデルは `src/client/lib/widgets.ts`（`WidgetLayout` = 左右それぞれの積む順の配列。スロット外＝そのウィジェット本来の既定位置）。エクスプローラーとアウトラインは既定位置を持たない `slotOnly` で必ずどちらかの枠に属し、正規化時に欠けていれば既定スロットへ補完する。設定は ⚙ ポップオーバーの「ウィジェット配置」から変更し `localStorage`（`nymph-widget-layout`）へ保存。タブは枠に置くと縦リスト（VSCode の Open Editors 相当）、コメントは枠に置くと高さドラッグを持たない `variant="slot"` になる。既定配置（左＝エクスプローラー / 右＝アウトライン）のレンダリング結果は移設前とピクセル同一
  - 表示するか（トグル）と配置（どの枠か）は別軸に保った。アウトラインの開閉はセッション限りで従来どおり永続化しない

---

## 完了（2026-07-21 セッション）

- **Phase 2 — コメントのライフサイクル（PR #115）**: コメント ID を `c_` + 乱数6桁hex に変更（既存整数 ID は非破壊共存）、`resolved`/`createdAt`/`round` を Comment に追加、コメントパネルに All/Open/Resolved フィルタ、チェックポイント設定を「ラウンド境界」として round を採番
- **全文検索（Quick Open 統合）**: mo の `/_/api/search` 相当の `GET /search?q=`（`src/search.ts`）。対象は開いているタブ + ツリー配下の .md で、ファイル名+本文を大文字小文字無視の部分一致で検索。前後1行のコンテキスト付きスニペット（長行はマッチ周辺にクリップしオフセット調整）、ファイルあたり5件・合計50件で打ち切り（`truncated`）。UI は Quick Open（Ctrl/Cmd+P）に「本文の一致」セクションとして統合し、選択すると該当ファイルを開いて対象行を含むブロックへスクロール+フラッシュ。クエリはデバウンス+2文字以上で発火（`useSearch`）

---

## 完了（2026-07-20 セッション）

- **Phase 1 — レビューデータの保存先移転（PR #111）**: コメント/チェックポイントをサイドカー方式から `$XDG_DATA_HOME/nymph/reviews/<key>/{comments.json, checkpoint}` へ移転。`<key>` = `sha256(ファイル絶対パス)` 先頭12桁の決定論的キー（`src/reviewStore.ts` の `reviewKey`）。comments.json は `{version: 2, file, updatedAt, comments}` のエンベロープ。書き込みは temp+rename のアトミック方式。レガシーサイドカーは読み取り/書き込み両経路で自動移行（移動方式・冪等）、破損ファイルは `*.corrupt-<timestamp>` に退避して原本を保全。`src/reviewStore.ts` が唯一の入出力窓口、`src/xdgPaths.ts` が XDG パス解決の共通ヘルパー
- **Stage A — パレット刷新（PR #112）**: ダーク=Tokyo Night 系（crit 由来: bg #0e0f13/#171922, text #c0caf5, accent #85aaf8）、ライト=GitHub Primer 系（mo 由来: #ffffff/#f6f8fa, text #1f2328, accent #0969da）。セマンティック色は `--danger`/`--success`/`--diff-*-bg` と `--*-rgb` に集約。highlight.js は tokyo-night-dark/github。本文フォントのデフォルトは Inter（切替機能と serif 選択肢は維持）
- **Stage B — ツールバー再編（PR #113）**: 常時表示9個+「⋯」オーバーフローメニュー（項目クリックで閉じる）+「⚙」設定ポップオーバー（調整系のため開いたまま）。接続状態と更新時刻はドット1個+ツールチップに統合。FileTabs はファイル2つ以上でのみ表示

---

## 設計原則

crit/mo 調査から得た指針。今後の機能追加でも守る。

- ツールの状態はレビュー対象リポジトリの外（XDG）に置き、対象を汚さない
- ID は採番せずハッシュ導出（決定論的キー）。コンテンツハッシュによる重複排除も同じ思想
- 軽いデータ（コメント）と重いデータ（スナップショット）はファイルを分ける
- 書き込みは常にアトミック（temp+rename）、マイグレーションは冪等に
- UI は「デフォルトで見せない、必要時に出す」。色はセマンティックトークン経由でのみ使う

---

## 次にやること（優先順）

1. **v0.5 — AI 統合** — v0.4（エクスポート & 出力）は 4 項目すべて完了。次のマイルストーンは下の「v0.5」節
2. **ウィジェットの残り宿題** — 配置画面のプレビューと「今は非表示」の注記は対応済み。残りは下の「残っている宿題」
3. **小粒 UX タスク** — キーボードショートカットは対応済み。残りは 2ファイル diff / `.nymph.toml`。下の表
4. **その他候補（優先度低）** — GitHub Alerts 対応 / レビューのコピー形式選択（JSON に加え Markdown/Text。`--annotate` の引用形式を画面のコピーにも回せる）/ stdin パイプ（`cat x.md | nymph`、コンテンツハッシュ重複排除）/ スクロール位置復元・見出しディープリンク

### ウィジェット（第2弾で出そろった）

枠に置けるウィジェットは 14 種。新しく足すときは `WIDGET_IDS` に id を、`WIDGET_META` に
ラベル・`grows`・`hint` を書き、`App.tsx` の `renderWidget` と `widgetPreview.ts` の
`previewOf`（配置画面に出す中身のプレビュー）に分岐を1つずつ足せば枠に載る。どちらも
`WidgetId` を網羅する switch なので、足し忘れは型エラーになる。

| ウィジェット | 状態 |
|---|---|
| タブ / エクスプローラー / アウトライン / コメント | 第1弾（既存パネルの移設） |
| 検索結果 / 最近・ブックマーク / ミニマップ / 図の一覧 / タスク / リンク・画像 / 用語集 / frontmatter / 差分サマリ / 文書統計 | 第2弾 |

**残っている宿題**
- 下スロット（今回は「左右のみ」で確定。コメントの下ドックは既定位置として残している）
- 配置画面のプレビューと「今は非表示」の注記は 2026-08-06 に対応済み（上の完了節）。残るのは、プレビューが**文字の一覧**であって実際の描画の縮小ではないこと。ミニマップの図やコメントの色分けのような「見た目そのもの」は伝わらない
- 図の一覧は種類と先頭行のテキストのみ。サムネイル（実際の描画の縮小）は mermaid の二重描画コストと枠の幅の制約から見送った
- frontmatter ウィジェットは本文側の表示には手を入れていない（「frontmatter の折りたたみ表示」は別テーマとして残っている）
- リンクの生死判定はレビュー対象の範囲内だけ。範囲外は「?」（未確認）のままで、判定範囲を広げるかは要検討

---

## 除外

やらないと決めたもの。

- **KaTeX/TeX 数式プレビュー: 実装しない**（ユーザー判断。2026-07-20）
- 共有（crit share 相当）: サーバー/認証が絡むため当面スコープ外
- ~~GitHub PR 同期~~: 除外から外した（2026-08-21）。`gh` に認証を外注する構成なら
  nymph はトークンを持たず待ち受けも増やさないため、除外の理由（サーバー/認証）が
  成立しない。別テーマとして [docs/ROADMAP-sync.md](docs/ROADMAP-sync.md) で進める
- **`--host` フラグ（LAN 公開）: 実装しない**（ユーザー判断。2026-07-22）認証機構が無いため、意図せず公開される事故のリスクの方が大きい。loopback 固定を維持する。
- **ヘッドレス CLI コメント（`nymph comment <file>:<line[-end]> "本文"`）: 実装しない**（ユーザー判断。2026-08-05）ブラウザなしのコメント追加・一覧・削除と、それに伴うエージェント連携の深化はスコープから外す。Claude Code 連携は既存の Edit フック（行番号リマップ）どまりとする。

---

## 開発運用メモ

- 完成の定義は E2E グリーン（CLAUDE.md）。`bun run test && bun run build && bun run test:e2e`
- VRT: ベースライン PNG は見た目に影響する変更時に**削除して CI 再生成**（`[skip ci]` コミットが自動で積まれる）。ローカルではフォント差でピクセル一致しない（既知）。VRT は `bun run test:e2e` に**含まれない**別構成（`playwright.vrt.config.ts`）なので、UI 挙動を変えたら `bunx playwright test --config playwright.vrt.config.ts` で「ベースライン差分以外の失敗がゼロ」を必ず確認する（PR #113 で横展開漏れの実績あり）
- E2E のレビューデータは `fixtures.ts` の `reviewCommentsPath` 等のヘルパー経由で新store を参照。テスト間の分離のため `reviewDir` を `beforeEach`/`afterEach` で掃除する
- メニュー操作ヘルパー（`openOverflowMenu`/`openSettingsMenu`）の挙動を変える際は `*_vrt.test.ts` を除外せず grep で全対象を列挙すること

---

## 小粒 UX タスク（優先度低）

| # | 機能 | 優先度 |
|---|------|--------|
| 1 | ~~**キーボードショートカット** — `?` でショートカット一覧表示、`C` でコメントパネル開閉、`T` でテーマ切替~~ **完了（2026-08-13）** | — |
| 2 | **2ファイル diff** — `nymph a.md b.md --diff` で 2 ファイルの追加 / 削除行をハイライト（チェックポイント diff とは別軸） | 中 |
| 3 | **設定ファイル対応** — `.nymph.toml` でポート・テーマ・ポーリング間隔をプロジェクト単位で指定 | 低 |

---

## v0.4 — エクスポート & 出力

**目標:** レビュー結果を別ツールへ流す

| # | 機能 | 優先度 |
|---|------|--------|
| 1 | ~~**HTML エクスポート** — `nymph report.md --export out.html` でコメント埋め込みの静的 HTML を生成~~ **完了（2026-08-05）** | — |
| 2 | ~~**PDF 出力** — 印刷用 CSS を追加し、ブラウザ印刷 API で PDF 生成~~ **完了（2026-08-06）** | — |
| 3 | ~~**コメントを Markdown に書き戻し** — レビューコメントをブロック直下に `> [nymph] …` 形式で挿入するオプション~~ **完了（2026-08-09）** | — |
| 4 | ~~**JSON → CSV エクスポート** — 保存済みコメントを CSV に変換する CLI サブコマンド `nymph export`~~ **完了（2026-08-09）** | — |

> 4 項目とも CLI から始めたが、画面（⋯ → エクスポート）からも同じ 3 形式を落とせるようにした
> （2026-08-13）。中身は共有しているので、経路によって出力が変わることはない。
>
> 印刷 / PDF は「アプリ画面の印刷（⋯ → 🖨 印刷 / PDF）」と「エクスポート HTML の印刷」の
> 2 経路。前者は本文だけ、後者はコメント込みで、どちらもブラウザの印刷から PDF にできる。
>
> 印刷の残り宿題:
> - コメントを紙に載せる経路はエクスポート HTML のみ（アプリからの印刷は本文だけ）
> - ヘッダー / フッター（ファイル名・ページ番号）は入れていない。`@page` のマージンボックスは
>   Chromium が未対応で、ブラウザ側の「ヘッダーとフッター」設定に任せている
>
> HTML エクスポートの残り宿題:
> - コードのシンタックスハイライトが付かない（hljs をサーバー側の実行時依存にするか要検討）
> - 生 HTML はエスケープ表示。アプリと同じ sanitize 結果にするには CLI 側に DOM（jsdom 等）が要る
> - 複数ファイルを 1 枚にまとめる出力は未対応（`--export` も `/export` もファイル1つのみ）
>
> Markdown 書き戻し / CSV の残り宿題:
> - 書き戻した Markdown から nymph へ**戻す**経路は無い（`> [nymph] …` を読んでコメントとして取り込む
>   inverse は未実装。書き戻しは一方通行）
> - 元ファイルへの上書き（in-place）は意図的に持たせていない。必要なら別ファイルへ書いてから
>   自分で差し替える
> - `--annotate` / `nymph export` とも対象はファイル 1 つ。ツリー配下をまとめて 1 つの CSV にする
>   出力は未対応（CSV に `file` 列を持たせてあるので、複数回の出力を連結すれば足りる想定）
> - CSV の `target`（対象の文章）は 1 行に潰した要約。表やコードの中身そのものは入らない

---

## v0.5 — AI 統合

**目標:** AI が生成した Markdown を AI がレビューするループを閉じる

| # | 機能 | 優先度 |
|---|------|--------|
| 1 | **Claude API 連携** — ツールバーに「AI レビュー」ボタン、ファイル内容を Claude に送り自動コメントを挿入 | 高 |
| 2 | **プロンプト付きレビュー** — レビュー観点（整合性チェック / 日本語校正 / 図の妥当性）をプリセット選択 | 中 |
| 3 | **コメントへの AI 返答** — 既存コメントを AI に見せ、修正案を提示させる | 低 |

> 実装時は `ANTHROPIC_API_KEY` が未設定なら AI ボタンを無効化し、外部依存は optional deps に閉じる。

---

## v1.0 — 配布 & エコシステム

**目標:** OSS として広く使われる状態にする

| # | 機能 |
|---|------|
| 1 | **GitHub Actions テンプレート** — PR に Markdown が含まれる場合、nymph で自動レビューを実行して結果をコメントするワークフロー |
| 2 | **VS Code 拡張** — エディタのサイドパネルで nymph プレビューを開く拡張 |
| 3 | **ドキュメントサイト** — GitHub Pages で機能紹介 + スクリーンショットを公開 |
| 4 | **プラグイン API** — カスタムブロックレンダラーを外部パッケージとして追加できる仕組み |

---

## 技術的負債 / インフラ

随時対応する項目。

| 項目 | 内容 |
|------|------|
| テストカバレッジ拡充 | テキスト選択コメント・draw.io ダウンロード・ドラッグ&ドロップ系のコメント保存を E2E でカバー |
| アクセシビリティ | `aria-*` 属性の整備、キーボードのみで全操作できるようにする |

### 完了済み

- **ファイルウォッチ方式の改善**: `handleWatch` を `fs.watch`（Bun 内部で FSEvents / inotify）ベースに切替。500ms ポーリングを廃止し、書き込みは native 通知で即座に SSE emit。安全網として 2s 間隔の stat 比較を残す（fs.watch の取りこぼしと SSE 再接続時の flush race を補償）。既存 E2E ホットリロードテスト全通過。
- **`/edit-op` フック python3 依存の除去**: `.claude/commands/install.md` の hook.sh を `jq` ベースに置換。同一 JSON 入力パターン（`tool_input` ラッパーあり/なし・非 JSON）をカバー。
- **hljs テーマ CSS の同梱**: `src/client/App.tsx` の CDN 直リンクを `import '?url'` に変更し `dist/assets/{tokyo-night-dark,github}.min-<hash>.css` として bundle。オフライン利用可・E2E の CDN mock 撤去。

---

## 優先順位の考え方

```
完了: npm 公開 → データ移転 (Phase 1) → UX 刷新 (Stage A/B)
      → Phase 2 コメントライフサイクル → 全文検索
      → ウィジェット配置 (第1弾 → 配置画面 → 第2弾 → 枠の幅ドラッグ)
      → HTML エクスポート (v0.4 #1) → 印刷 / PDF (v0.4 #2)
      → Markdown 書き戻し (v0.4 #3) → CSV エクスポート (v0.4 #4)
      → 画面からのエクスポート → キーボードショートカット
                                      ↓ 現在ここ
次にやる: AI 統合 (v0.5) → エコシステム (v1.0)
          （合間に: ウィジェットの残り宿題 / 小粒 UX タスク）
```

各マイルストーンは独立しており、どこからでも着手できます。  
フィードバックや優先度の変更があれば随時このファイルを更新してください。
