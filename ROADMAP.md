# nymph ロードマップ

> 現在バージョン: **v0.2.0**  
> ツールの方針: 依存ゼロ（Python 標準ライブラリのみ）、シングルコマンド、軽量

---

## 現状の機能（v0.2.0 相当）

**v0.1.0 で実装済み**

- Markdown ホットリロード（SSE + 500ms ポーリング）
- Mermaid ダイアグラムレンダリング
- draw.io エクスポート（`.drawio` ダウンロード / コードコピー）
- ブロック単位インラインコメント（行番号紐づけ・JSON 保存）
- レビューのクリップボードコピー
- ダーク / ライトテーマ切替

**v0.2 で追加実装済み**

- **コメント編集** — コメントパネルの ✎ ボタンからインライン編集（`openEditModal`）
- **スクロール同期** — コメントパネルのアイテムをクリックで該当ブロックへスムーススクロール＋フォーカスハイライト（Mermaid ブロック含む）
- **テキスト選択コメント** *(ロードマップ外)* — コンテンツ上のテキストを選択するとポップアップが表示され、選択範囲を context として記録するコメントを追加可能
- **Claude Code hook 連携** *(ロードマップ外)* — Claude Code が Markdown を編集した際に `/edit-op` エンドポイント経由でコメントの行番号を自動リマップ

---

## v0.2 — UX の底上げ ✅ 完了

**目標:** 毎日使いたくなる細部を詰める

| # | 機能 | 優先度 | 状態 |
|---|------|--------|------|
| 1 | **コメント編集** — コメント一覧からインライン編集できるようにする（現状は追加・削除のみ） | 高 | ✅ 実装済み |
| 2 | **スクロール同期** — コメントパネルのアイテムをクリックしたとき該当ブロックまでスムーススクロールし、フォーカスリングを表示 | 高 | ✅ 実装済み |
| 3 | **コードシンタックスハイライト** — `highlight.js` を CDN 追加、fenced code block を言語別着色 | 中 | ❌ 未実装 |
| 4 | **キーボードショートカット** — `?` でショートカット一覧表示、`C` でコメントパネル開閉、`T` でテーマ切替 | 中 | ❌ 未実装 |
| 5 | **コメント検索 / フィルタ** — コメントパネル内で文字列検索、行番号でソート維持 | 低 | ❌ 未実装 |

---

## v0.3 — ファイル操作の拡張

**目標:** 単一ファイル縛りを外す

| # | 機能 | 優先度 |
|---|------|--------|
| 1 | **複数ファイルウォッチ** — `nymph *.md` や `nymph docs/` でディレクトリ内の `.md` を一括監視、タブ切替 UI | 高 |
| 2 | **差分ビュー** — `nymph a.md b.md --diff` で 2 ファイルの追加 / 削除行をハイライト表示 | 中 |
| 3 | **ドラッグ & ドロップ** — ブラウザ上にファイルをドロップすると監視対象を切替（CLI 引数なしで起動可能に） | 中 |
| 4 | **設定ファイル対応** — `.nymph.toml` でポート・テーマ・ポーリング間隔をプロジェクト単位で指定 | 低 |

---

## v0.4 — エクスポート & 出力

**目標:** レビュー結果を別ツールへ流す

| # | 機能 | 優先度 |
|---|------|--------|
| 1 | **HTML エクスポート** — `nymph report.md --export out.html` でコメント埋め込みの静的 HTML を生成 | 高 |
| 2 | **PDF 出力** — ブラウザの印刷 API を使いスタイルを最適化した印刷用 CSS を追加 | 中 |
| 3 | **コメントを Markdown に書き戻し** — レビューコメントを元ファイルのブロック直下に `> [nymph] …` 形式で挿入するオプション | 中 | ✅ 実装済み |
| 4 | **JSON → CSV エクスポート** — `.comments.json` を CSV に変換する CLI サブコマンド `nymph export` | 低 |

---

## v0.5 — AI 統合

**目標:** AI が生成した Markdown を AI がレビューするループを閉じる

| # | 機能 | 優先度 |
|---|------|--------|
| 1 | **Claude API 連携** — ツールバーに「AI レビュー」ボタン、ファイル内容を Claude に送り自動コメントを挿入 | 高 |
| 2 | **プロンプト付きレビュー** — レビュー観点（整合性チェック / 日本語校正 / 図の妥当性）をプリセット選択 | 中 |
| 3 | **コメントへの AI 返答** — 既存コメントを AI に見せ、修正案を提示させる | 低 |

> 実装時は `ANTHROPIC_API_KEY` が未設定なら AI ボタンを無効化し、外部依存は `pip install nymph[ai]` の optional deps に閉じる。

---

## v1.0 — 配布 & エコシステム

**目標:** OSS として広く使われる状態にする

| # | 機能 |
|---|------|
| 1 | **PyPI 公開** — `pip install nymph` で入る状態にする、`uv tool install nymph` の動作確認 |
| 2 | **GitHub Actions テンプレート** — PR に Markdown が含まれる場合、nymph で自動レビューを実行して結果をコメントするワークフロー |
| 3 | **VS Code 拡張** — エディタのサイドパネルで nymph プレビューを開く拡張（webview で既存 HTML を流用） |
| 4 | **ドキュメントサイト** — GitHub Pages で機能紹介 + スクリーンショットを公開 |
| 5 | **プラグイン API** — カスタムブロックレンダラーを外部 Python パッケージとして追加できる仕組み |

---

## 技術的負債 / インフラ

随時対応する項目。

| 項目 | 内容 |
|------|------|
| ファイルウォッチ方式の改善 | 500ms ポーリングを `watchdog` ライブラリ（optional dep）または `kqueue` / `inotify` 直叩きに置換し CPU 消費を削減 |
| テストカバレッジ拡充 | E2E でカバーされていないコメント編集・draw.io ダウンロードを追加 |
| `marked.js` バンドル | CDN 依存をなくしオフライン利用を可能にする（`nymph/static/` に同梱） |
| アクセシビリティ | `aria-*` 属性の整備、キーボードのみで全操作できるようにする |
| セキュリティ | ✅ `localhost` バインドは明示済み。`--host` フラグで LAN 共有を opt-in 化（未実装） |

---

## 優先順位の考え方

```
✅ 使いやすさ向上 (v0.2) → ファイル拡張 (v0.3) → 出力強化 (v0.4)
                                ↑ 現在ここ
AI 統合 (v0.5) は使いやすさが固まってから
     ↓
PyPI 公開 (v1.0) は機能が安定したタイミングで
```

各マイルストーンは独立しており、どこからでも着手できます。  
フィードバックや優先度の変更があれば随時このファイルを更新してください。
